{
  "name": "omp-semantic-policy",
  "version": "0.1.0",
  "description": "Semantic policy enforcement for Oh My Pi",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/goulinkh/omp-semantic-policy.git"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "packageManager": "bun@1.4.2",
  "files": [
    "dist",
    "src/policy"
  ],
  "exports": {
    ".": {
      "types": "./src/policy/index.ts",
      "import": "./dist/policy/index.js"
    },
    "./omp": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "omp": {
    "extensions": [
      "./dist/index.js"
    ],
    "settings": {
      "showStatus": {
        "type": "boolean",
        "default": true,
        "description": "Show semantic policy state in OMP's native status bar."
      },
      "showViolationFeedback": {
        "type": "boolean",
        "default": true,
        "description": "Show action-bound feedback for direct actions and headless workflow checks. Blocked tool calls always include policy feedback in their originating tool card, without session-wide notifications."
      },
      "confirmationDefault": {
        "type": "enum",
        "values": [
          "deny",
          "approve"
        ],
        "default": "approve",
        "description": "Resolve confirmation requests automatically when no dialog is selected. Approve (default) accepts uncertainty, including ungrounded model denials and unavailable-evaluator fallbacks; deny opts into fail-closed behavior. Grounded policy denials remain blocked."
      },
      "confirmationThreshold": {
        "type": "number",
        "default": 1,
        "min": 0,
        "max": 1,
        "step": 0.05,
        "description": "The default 1 keeps decisions automatic without dialogs, using confirmationDefault (approve by default). Set below 1 to prompt at or above this confidence; 0 prompts for every uncertain checked action. Interactive session-stop checks are skipped to protect the next draft; headless workflow checks resolve automatically. Explicitly selected prompts block without an interactive UI."
      },
      "enabledToolCalls": {
        "type": "string",
        "default": "bash,eval,python,write,edit,task,hub,browser,computer,debug",
        "description": "Comma-separated exact OMP tool names for remote semantic evaluation. Defaults to mutation, execution, delegation, and process-control tools; routine inspection and lsp (including write to xd://lsp) skip TypeSafe. Local path protections remain active. An empty allowlist disables remote semantic evaluation for registered tool calls."
      },
      "disabledToolCalls": {
        "type": "string",
        "default": "",
        "description": "Comma-separated exact OMP tool names that skip semantic evaluation, including completeness checks. Grounded local path protections remain active. Empty keeps all current coverage defaults."
      },
      "toolOperations": {
        "type": "string",
        "default": "",
        "description": "Comma-separated exact tool classifications such as launchpad=read,launchpad_write=execute. Supported operations: read, write, execute, delegate, network, workflow, internal, and unknown. JSON object syntax is also accepted."
      }
    }
  },
  "scripts": {
    "build": "bun scripts/build.ts",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun test && bun run build",
    "format": "oxfmt --write --no-error-on-unmatched-pattern 'src/**/*.ts' 'scripts/**/*.ts' 'testing/**/*.ts' 'docs/assets/policy-flow/**/*.ts'",
    "format:check": "oxfmt --check --no-error-on-unmatched-pattern 'src/**/*.ts' 'scripts/**/*.ts' 'testing/**/*.ts' 'docs/assets/policy-flow/**/*.ts'",
    "lint": "oxlint src scripts testing docs/assets/policy-flow",
    "test": "bun test",
    "test:e2e:provider": "bun testing/e2e/provider-project.ts",
    "test:e2e:marketplace": "bun run build && OMP_MARKETPLACE_E2E=1 bun test testing/e2e/marketplace-install.test.ts",
    "tune:policy": "bun testing/e2e/policy-tuning.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@fontsource/ubuntu-mono": "^5.3.0",
    "@fontsource/ubuntu-sans": "^5.3.0",
    "@oh-my-pi/pi-coding-agent": "18.1.19",
    "@types/bun": "latest",
    "oxfmt": "latest",
    "oxlint": "latest",
    "satori": "^0.33.4",
    "typescript": "latest"
  },
  "dependencies": {
    "@oh-my-pi/pi-ai": "18.1.19",
    "@oh-my-pi/pi-natives": "18.1.19",
    "@oh-my-pi/pi-tui": "18.1.19",
    "@oh-my-pi/pi-utils": "18.1.19",
    "@typesafe-ai/sdk": "0.6.0"
  }
}
